async function mime_unsafe_html(content, ctx) ::
  content = await (await content)?.text()
  let el = ctx.imtiny('div', {})
  el.innerHTML = content ?? ''
  return el

export async function itermuse(ctx) ::
  ctx.mimeview.set('text/html', mime_unsafe_html)

